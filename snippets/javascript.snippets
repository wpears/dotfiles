# for loop copy
snippet for
	for(var ${2:i}=0, l=${1:arr}.length; $2<l; $2++){
		var ${3:v} = $1[$2];${4:}
	}
	${0}

# forEach
snippet fe
	${1:arr}.forEach(${2:v},${3:i}){
		${4};
	});
	${0}

# map
snippet m
	${1:arr}.map(${2:v},${3:i}){
		return ${4};
	});
	${0}
	
# assign map
snippet vm
	var ${1:mapped} = ${2:arr}.map(${3:v},${4:i}){
		return ${5}
	});
	${0}
